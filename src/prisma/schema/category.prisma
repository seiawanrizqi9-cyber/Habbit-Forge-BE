model Category {
  id          String   @id @default(uuid())
  name        CategoryName   @unique
  createdAt   DateTime @default(now())
  color       String?
  description String?
  icon        String?
  habit       Habit[]

  @@map("categories")
}

enum CategoryName {
  HEALTHY
  FINANCE
  WORK
  LEARNING
  SOCIAL
}